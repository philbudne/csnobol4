.\" -*-nroff-*-
.\" $Id$
.TH SNOBOL4 1 "6 September 1996" "Local"
.SH NAME
snobol4 \- SNOBOL4 interpreter
.SH SYNOPSIS
.B snobol4
[
.I options
\&.\|.\|.
]
[
.I files
\&.\|.\|.
]

.SH DESCRIPTION
.PP
This manual page describes a port of the original Bell Telephone Labs
(BTL) Macro Implementation of SNOBOL4 (MAINBOL) to machines with
32-bit C compilers by Philip L. Budne.  The language and it's
implementation are described in [1] and [2].  Many extensions from one
or more of Catspaw Vanilla SNOBOL4, Catspaw SNOBOL4+, SPITBOL and
SITBOL have been added.

.SS Command line
The files on the command line are read in turn until an
.B END
label is seen.  If no files are specified, a program (terminated by an
.B END
label) is read from standard input.

.SS Limitations
.PP
All aspects of the language are implemented except;
.IP *
Trapping of arithmetic exceptions.
.IP *
Dynamic use of the
.B LOAD()
function on non-BSD platforms.  User functions can be staticly linked
(poor man's loading) on ALL platforms.  Read load.doc for more
information.

.SS "Changes"
.PP
The following behaviors have been changed;
.IP *
Listings are disabled by default.
Default listing side (when enabled by
.BR -LIST
or the
.B -l
option is
.BR LEFT .
Listings are directed to standard output.
.IP *
Character set (see below).
.IP *
Output to the
.B PUNCH
variable is directed to standard error.
.IP *
I/O is not performed using
.B FORTRAN
I/O.  The format argument to the
.B OUTPUT()
function is ignored.
.IP *
Control lines and comment characters are valid after the end of string
(;) statement separator.  Listing statement numbers show the statement
number of the LAST statement on the line (rather than the first).
.IP *
Setting the
.B &ABEND
keyword causes a core dump upon termination!
.IP *
The value of the
.B &CODE
keyword determines the exit status of the
.B snobol4
application.

.SS "Character set"

The internal character set and collating sequence are USASCII-8.  Any
8-bit byte pattern is accepted as a SNOBOL datum or in a string
constant of a SNOBOL source program.  The value of the SNOBOL
protected keyword
.B &ALPHABET
is a 256-character string of all Bytes from 0 to 255, in ascending
order.  Programs may be entered in mixed case.  By default lower case
identifiers are folded to upper case (see
.B &CASE
and
.B -CASE
extensions).
.PP
The following operator character sequences are permitted and represent
a cross between PDP-10 MAINBOL (DECBOL), SITBOL and Catspaw SPITBOL
usage;

.ta 2i
.nf
exponentiation	^\0**
alternation	|\0!
unary negation	~\0\\
assignment	=\0_
comment line	*\0#\0|\0;\0!
continuation	+\0.
.fi
.PP
Both brackets ([]) and widgits/angle-brackets (<>) may be used to
subscript arrays and tables.  The
.B TAB
(ASCII 9) character is accepted as a space, but since the compiler
does not expand them, it's use may cause confusion in listings and
error messages.  Note that the use of the pound sign for comments
allows use of the shell interpreter sequence at the top of a file (ie;
"#!/usr/local/bin/snobol4 -b").  Underscore (_) and period (.) are
legal within identifiers and labels.

.SS "Extensions"

.TP
.BR "SPITBOL " extensions
The
.B SPITBOL
scan (?) and assignment (=) operators have been added.  A patern match
can appear within an expression, and returns the matched string as
it's value.  Similarly assignment can appear in an expression, and
returns the assigned value. An assignment after a scan (ie; STRING ?
PATTERN = VALUE) performs a scan and replace.  Assignment is right
associative, and has the lowest precidence, while scan is left
associative and has a precidence just higher than assignment.

.br
The
.B SPITBOL
selection/alternative construction can be used in any expression.  It
consists of a comma seperated list of expressions inside parentheses.
The expressions are evaluated until one succeeds, and it's value is
returned.  Abuse of this construction may result in incomprehensible
code.

.br
The
.B NUMERIC
argument
to
.B CONVERT()
and the removal of leading spaces from strings converted (implcitly or
explicitly) to numbers are also legal when
.B SPITBOL
extensions are enabled.
.B SPITBOL
extensions can be enabled and disabled using the
.B "\-PLUSOPS"
directive.
.B "\-PLUSOPS 0"
disables SPITBOL operators, while
.B "\-PLUSOPS" 
or
.BI "\-PLUSOPS " n
where
.I n
is a non-zero integer enables them.  SPITBOL extensions are enabled by
default.

.TP
Case folding
.br
By default the compiler folds identifiers and directives
to upper case, so programs can be entered in either case.  To disable
case folding use the directive
.B "\-CASE 0" or
.BR "\-CASE" .
To reenable case folding use directive
.BI "\-CASE " n
where
.I n
is a non-zero integer.  The status of case folding may be examined and
controlled from a running program by the unprotected system keyword
.BR &CASE .

.TP
Named files
.br
Filenames
can be supplied to the
.B INPUT()
and
.B OUTPUT()
functions via an optional fourth argument.  If the filename begins
with a vertical bar (|), the remainder is used as a shell command
whose stdin (in the case of
.BR OUTPUT() )
or stdout (in the case of
.BR INPUT() )
will be connected to the file variable.  The magic filenames
.BR /dev/stdin ,
.BR /dev/stdout ,
and
.B /dev/stderr
refer to the current process standard input, standard output and
standard error respectively regardless of whether those special
filenames exist on your system.

.TP
No statement limit
.br
Keyword
.B &STLIMIT
now defaults to -1, which is interpreted as no limit to the number of
statements executed.

.TP
.B &UCASE/&LCASE
.br
Protected keywords
.B &UCASE
and
.B &LCASE
contain upper and lower case characters respectively.

.TP
.B \-ERROR/\-NOERRORS
.br
Directives
.B \-ERROR
and
.B \-NOERRORS
control execution of program with compiler errors.  If the
.B \-ERROR
directive is given, the program will be executed (but any attempt to
execute a statement with a compiler error will cause a fatal execution
error.  By default programs with compiler errors will not be started,
this can be restored using
.BR \-NOERRORS .

.TP
.B \-INCLUDE
.br
The
.B \-INCLUDE 
directive causes the compiler to interpolate the contents of the named
file enclosed in single or double quotes.  Trailing spaces are removed
from the filename before use. Any filename will be included only once,
this can be overridden by appending a trailing space to the filename.
.B \-COPY is a synonym for
.B \-INCLUDE
for compatibility with
.BR SPITBOL/370 .

.TP
.B BREAKX()
.br
The
.B BREAKX()
function is a pattern function used for fast scanning.
.B BREAKX(str)
is equivalent to
.B "BREAK(str) ARBNO(LEN(1) BREAK(str))"

.TP
.B CHAR()
.br
The
.B CHAR()
function takes an integer from 0 to 255 and returns the n'th character
in
.B &ALPHABET .

.TP
.B LPAD()/RPAD()
.br
The
.B LPAD()
and
.B
.B RPAD()
functions take the first argument (subject) string, and pad it out to
the length specified in the second argument, using the first character
of the optional third argument.  If the third argument is missing, or
is the null string. spaces will be used for padding.  The subject will
be returned unmodified if already long enough.

.TP
.B SORT()/RSORT()
.br
The
.B SORT()
and
.B
.B RSORT()
functions take a array (or table, which is first converted to an
array), and sorts the rows according to the value of the array column
given in the second argument (for a table, a second argument of 1
sorts by table indecies, an argument of 2 sorts by table element
values). 
.B SORT()
sorts elements in ascending order, while
.B RSORT()
sorts in decending order.

.TP
.B FREEZE()/THAW()
.br
The
.B FREEZE()
and
.B
.B THAW()
functions prohibit new entry creation in named table.  This is useful
once a table has been initialized to avoid the creation of empty
entries that normally occurs on table entry lookup failure.

.TP
.Exponents
.br
.B REAL
number syntax has been expanded to allow exponents
of the form
.IR "ANY('DeEe') ('+' | '-' | '') SPAN('0123456789')" .

.SS "I/O Associations"

.br
I/O is performed by associating a variable name with a numbered I/O
unit using the
.B INPUT()
and
.B OUTPUI()
functions.  The following associations are available by default;

.br
.ta 1i 2i
.nf
Variable	Unit	Association
\fBINPUT\fR	5	standard input
\fBOUTPUT\fR	6	standard output
\fBPUNCH\fR	7	standard error
.fi

.SH OPTIONS
.TP 1i
.B \-b
Toggle startup banner output (by default on).
.TP
.BI -d NNN
Allocate dynamic region of
.I NNN
descriptors (default is 25000).  A larger dynamic region may result in
fewer garbage collections (storage regenerations), however large
values may cause collections (when they occur) to run more slowly.
Most programs do not need an increased dynamic region to run.
.TP
.B -f
Toggle folding of identifiers to upper case (see
.B \-CASE
and
.BR &CASE ).
.TP
.B -k
Toggle running programs with compilation errors (see
.B \-ERROR
and
.B \-NOERRORS
extensions).  By default programs with compilation errors will not be
run.
.TP
.B -l
Reenable listing to stdout.  (default is
.BR \-UNLIST ).
Default listing side is
.BR LEFT .
.TP
.B -p
Toggle
.B SPITBOL 
extensions (also controlled by
.BR \-PLUSOPS ).

.TP
.B -r
Toggle reading
.B INPUT
from input file(s) after
.B END
label.  Otherwise
.B INPUT
defaults (back) to standard input after program compilation is complete.
.TP
-s
Toggle termination statistics (off by default).

.SH SEE ALSO
.IP [1]
R. E. Griswold, J. F. Poage, and I. P. Polonsky
.IR "The SNOBOL4 Programming Language" ,
2nd ed., Prentice-Hall Inc., 1971.
.IP [2]
R. E. Griswold,
.IR "The Macro Implementation of SNOBOL4" ,
W. H. Freeman and Co., 1972.

.SH AUTHOR
Philip L. Budne

.SH BUGS
I/O retains some record oriented flavor.
.br
I/O is still tied to unit numbers.
.br
Compiler error messages refer to listing line number (not source file line).
.br
Listings don't deal well with
.B TAB
characters.
.br
Dynamic storage cannot be expanded after startup.
.br
Listing lines are space padded to 132 characters per line.
