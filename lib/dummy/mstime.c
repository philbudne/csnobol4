/* $Id$ */

#ifdef HAVE_CONFIG_H
#include "config.h"
#endif /* HAVE_CONFIG_H defined */

#include "snotypes.h"

real_t
mstime() {
    /* use time() to get elapsed time? need to keep base (see borland vers) */
    return 0.0;
}
